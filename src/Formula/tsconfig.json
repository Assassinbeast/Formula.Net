{
  "disableCompileOnSave": true,
  "compilerOptions": {
    //todo: when "module: xxx" property is deleted, 
    //error happens in formula.ts in dynamic import. 
    //Also, we need to turn this on in gulpfile.js when compiling, right?
    "module": "commonjs",
    "target": "ES6"
  }
}

